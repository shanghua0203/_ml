你是專業工程師。我剛剛已經成功寫出一個「非 Transformer (完全沒有 Attention)」的 LSTM 語言模型大雜燴程式碼（我會貼在最下面）。
現在我要進入第二階段的「模組化分家」，請幫我把下面這坨混在一起的程式碼，拆解整理成以下 3 個乾淨的檔案：
	1.	text_processor.py (資料處理小幫手)：把負責「建立字典」、「文字變數字」的函數或類別放在這裡。
	2.	model.py (模型工廠)：只放我那個沒有 Attention 的 LSTM 模型架構。
	3.	main.py (主程式與老闆)：負責把前面兩個檔案 import 進來，然後執行「訓練迴圈」跟最後的「文字生成」。
⚠️遵守以下 3 個規則：
	1.	白話註解：拆開後的檔案裡面，請繼續保留或加上白話文註解（例如：這個檔案是倉庫、那個檔案是大腦）。
	2.	教我怎麼跑：寫一個README,請在最後用最簡單的步驟告訴我，這三個檔案建好之後，我要在終端機（Terminal）輸入什麼指令才能讓它跑起來。
