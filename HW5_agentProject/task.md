請幫我修改 agentSecure.py。目前的 Agent 可以執行任何 shell 命令，這有安全風險。我希望增加「檔案存取安全控管」：
-

1. 建立安全檢查函式：新增一個 is_path_safe(command) 函式。它必須解析 <shell> 標籤內的命令，找出所有涉及的路徑。

2. 路徑檢查邏輯：

    - 使用 os.path.abspath 將路徑轉為絕對路徑。

    - 檢查該路徑是否位在 WORKSPACE (即 ~/.agentSecure) 資料夾內。

    - 必須防範使用 .. 或 ~/ 試圖跳出資料夾的行為。

3. 攔截與詢問：

    - 在 main() 迴圈執行 subprocess.run 之前，先呼叫 is_path_safe。

    - 如果發現存取了 WORKSPACE 以外的檔案，必須在終端機顯示：⚠️ 安全警告：Agent 試圖存取外部檔案 [檔案路徑]，是否核可？(y/n): 。

    - 只有使用者輸入 y 時才繼續執行；若輸入 n，則跳過該命令並回傳「存取遭拒絕」給 Agent。

4. **寬鬆處理**：一般的系統命令（如 ls, echo, date）如果不帶路徑則直接允許，但只要有檔案操作（如 cat, rm, cp, mv, python3 指向某個檔案）就必須檢查。

請直接修改程式碼並保持原本的記憶功能與邏輯。