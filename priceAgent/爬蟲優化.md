# 你現在是一個專精於網頁爬蟲與自動化的 Python 工程師，熟悉 Crawl4AI 與 Playwright。
# 請幫我優化 `src/agents/scrape_agent.py` 中的網頁抓取邏輯，增加「擬人化」的反爬蟲機制。

【目前狀況】
-
目前的 `ScrapeAgent` 使用 Crawl4AI 進行簡單抓取，容易被嚴格的購物網站（如蝦皮或 momo）偵測並封鎖。

【我要新增的「變裝」功能】
-
1. **偽裝身份 (User-Agent 輪換)**：在抓取時，隨機切換不同的真實瀏覽器代號（User-Agent），不要每次都用同一個。
2. **行為擬人化 (Human-like behavior)**：
   - **進入後等待**：進到網頁後，先隨機等待 2~5 秒，不要立刻抓資料。
   - **隨機捲動 (Random Scroll)**：模擬人類滑動滑鼠，隨機向下捲動頁面，再向上捲動一點點，確保觸發網頁的「懶載入 (Lazy Loading)」內容。
   - **模擬滑鼠移動**：如果可以，加入簡單的滑鼠隨機移動軌跡。
3. **繞過檢查 (Bypass detection)**：開啟 Crawl4AI 的 `magic_mode` 或相關的防偵測參數（如 `headless=False` 的模擬或 stealth 模式）。

【具體實作要求】
-
- 請修改 `scrape` 函數，在呼叫 `crawler.arun` 之前或之中，加入自定義的 JavaScript 執行腳本 (js_code) 來實現隨機捲動。
- 使用 Crawl4AI 的 `CrawlerRunConfig` 來設定這些擬人化參數。
- 確保程式碼中包含隨機時間 (random.uniform) 的延遲，避免規律性。
- 修改後請保持與原本 `ScrapeResult` 回傳格式的相容性。

- **請給我修改後的完整 `ScrapeAgent` 類別程式碼，並在關鍵地方加上繁體中文註解。**