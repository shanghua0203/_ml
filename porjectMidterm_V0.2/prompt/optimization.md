你現在是一位資深且極度嚴格的 PyTorch 軟體工程師。
我目前有一個「非 Transformer 的 LSTM 中文語言模型（文字接龍）」專案。
請幫我對現有專案進行「專業化升級」，並嚴格遵守以下【技術優化方向】與【開發規範】。

⚠️ 絕對限制：專案中絕對禁止使用 Transformer、Attention 或是 Multi-Head Attention 機制。

【第一部分：技術優化方向】
請幫我把以下的優化技術實作進程式碼中：
1. 模型架構優化（權重共享）：在不使用雙向 LSTM 的前提下，實作「Weight Tying（權重共享）」，讓 Embedding 層和 Linear 輸出層共用同一組權重，讓模型學得更好且減少體積。
2. 文字預處理優化（詞層級與防呆）：引入 `jieba` 套件將字層級改為「詞層級（Word-level）」切分。同時，在字典中加入 `<UNK>` 標籤，如果遇到沒看過的詞就用 `<UNK>` 替換，防止程式報錯當機。
3. 效能優化（硬體加速）：在訓練與生成時，加入自動偵測並推送到 GPU (CUDA) 或 Apple Silicon (MPS) 的邏輯，不要只綁死在 CPU 上。
4. 文字生成演算法優化（Top-k）：在 `generate_text` 函數中，除了原本的 Temperature，請再加入「Top-k 採樣（預設 k=5）」，只從最熱門的前 k 個選項中抽樣，避免模型產出無意義的火星文。

【第二部分：嚴格開發規範】
在改寫或新增程式碼時，你必須 100% 遵守以下規則：
1. 完整的測試保護：必須針對新功能與舊功能，撰寫適當的「單元測試（Unit Test）」與「系統測試（System Test）」。
2. 一鍵測試腳本：必須寫一個 `test.sh` 的 Bash 腳本，讓我可以一鍵執行專案內的所有測試。
3. 強制模組化：任何單一 Python 檔案如果超過 1000 行，就必須強制拆分成多個模組檔案，保持專案乾淨。
4. 詳細說明書：必須撰寫一份非常詳細的 `README.md`，說明專案架構、如何安裝套件（如 jieba）、如何執行訓練與生成。
5. 語言與白話限制：程式碼內的「所有註解」以及 `README.md` 都【必須使用繁體中文】。而且註解必須「極度白話」，把所有的專業名詞（例如 Top-k、Weight Tying、UNK）用連國小生都能懂的日常比喻來解釋。
6. 版本歷史紀錄：必須在專案中建立一個 `_doc/` 資料夾。每一次的版本更新，都要在裡面寫一個 `vX.y.md`（例如：v1.1.md、v1.2.md，每次進版前進 0.1），在裡面詳細記錄這次版本改了哪些東西。

請清楚、完整地輸出修改後的程式碼、測試檔案、腳本與文件內容。
