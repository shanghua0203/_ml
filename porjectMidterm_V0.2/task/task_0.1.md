你現在是一個專業的程式設計師。製作一份「語言模型」的作業，請給我一份完整的 Python (PyTorch) 程式碼。
嚴格遵守以下 5 個規則，不然我作業會被當掉：
	1.	絕對禁止：絕對不能用 Transformer、Attention 或 Multi-Head Attention 機制。請用最經典的 LSTM 來做。
	2.	大雜燴風格：請把所有的程式碼都寫在「同一個 Python 檔案」裡面，不要拆分成多個檔案，我要可以直接從頭跑到尾。
	3.	完整四步驟：程式碼裡必須包含這四個部分：
•	(1) 文字預處理：幫我寫一個超簡單的函數，把中文字變成數字字典。
•	(2) LSTM 模型架構：蓋一個簡單的 Embedding + LSTM + Linear 網路。
•	(3) 訓練迴圈：要有優化器、計算 Loss，並且每跑幾個回合就要印出 Loss 讓我看它有在變聰明。
•	(4) 文字生成：訓練完後，寫一個小功能，給它一個開頭的字，它可以自己生出後面的句子。
	4.	超級白話的註解：程式碼裡面的每一行註解，請把我當作完全沒有基礎，用最白話的比喻來解釋這行程式碼在幹嘛。
	5.	自帶假資料：請在程式碼最前面，自己隨便編一個 50 字左右的中文小故事當作訓練資料，確保我把程式碼複製貼上後，按下執行就能馬上看到結果。
