你現在是一位資深軟體工程師，正在協助一個毫無基礎的新手完成「非 Transformer 語言模型（LSTM）」的期末作業收尾。目前專案已經模組化完畢。為了讓這份作業達到滿分標準，請幫我加上專業的測試流程。
請保持你的回答專業且簡潔，但程式碼的註解與概念解釋必須「極度白話」（例如把測試比喻成工廠的品管檢查），並嚴格為我產出以下三個檔案的內容：
	1.	單元測試與系統測試代碼 (例如 test_main.py)：
•	單元測試 (Unit Test)：請幫我寫測試案例，單獨檢查「文字轉數字的函數」和「LSTM 模型前向傳播（Forward pass）」。驗證它們輸出的格式與大小（Shape）對不對。（請白話解釋：這就像檢查單一齒輪和馬達有沒有瑕疵）。
•	系統測試 (System Test)：請幫我寫一個測試案例，模擬一次完整的「餵入文字 -> 訓練一步 -> 預測下一個字」的流程，確保程式不會跑一跑當機。（請白話解釋：這就像把車子組裝好後，發動引擎開一圈看看）。
•	註：請使用內建的 unittest 或是主流的 pytest 框架來寫。
	2.	自動化測試腳本 (test.sh)：
•	請給我一個 bash 腳本，裡面包含一鍵執行上述所有測試的指令。
•	請在腳本內加上註解，說明如果在沒有權限的時候該怎麼辦。
	3.	傻瓜操作手冊：
•	請在最後用簡單兩句話告訴我，我該在小黑窗（終端機）裡輸入哪兩行指令，才能給予 test.sh 執行權限並順利讓它跑起來（例如 chmod +x 的白話文解釋）。
